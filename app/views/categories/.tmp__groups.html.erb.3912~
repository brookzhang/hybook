<% @groups.each do |group|%>
  <%if group.id == @category.id%>
	<%= link_to(group.title,group,:class=>'current_category') %>
	<%#= link_to(group.title,group) %>
	<%else%>
  <%= link_to(group.title,group) %>
	<%end%>
	<br/>
  
<%end%>

